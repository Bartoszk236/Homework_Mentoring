spring:
  application:
    name: OrderSystemBusinessLayer
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres_main
    username: postgres
    password: P@ssword
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
