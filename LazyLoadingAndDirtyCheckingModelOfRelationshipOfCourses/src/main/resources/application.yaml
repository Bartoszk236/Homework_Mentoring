spring:
  application:
    name: LazyLoadingAndDirtyCheckingModelOfRelationshipOfCourses
  datasource:
    driver-class-name: org.h2.Driver
    name: sa
    password:
    url: jdbc:h2:mem:database
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
logging:
  level:
    root: debug
